<div class="main">
  <div class="option_explanation">
    <span>＊</span>設定を変更したいときは、以下のフォームを編集して保存してください。<br>
    <span>＊</span>目標と期限をクリアにしたいときは、空欄にして保存してください。<br>
    <span>＊</span>レートは必須です。（デフォルトは 1 分 ＝ 1 円です。）
  </div>
  <%= form_with model: @option, class: "option_form", local: true do |f| %>
    <% if @option.errors.any? %>
      <ul class="option_error_messages">
        <% @option.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>
    <div class="">
      <%= f.label :goal, "目標：" %>
      <%= f.text_field :goal, class: "goal_form", placeholder: "例）結婚式までに10万円貯める" %>
    </div>
    <div class="">
      <%= f.label :goal_day, "期限：" %>
      <%= f.date_field :goal_day, class: "goal_day_form" %>
    </div>
    <div class="rate_option">
      <%= f.label :rate, "レート：" %>
      <span>1分＝</span><%= f.number_field :rate, class: "rate_form", placeholder: "例）10", min: "1" %>円
    </div>
    <div class="option_submit">
      <%= f.submit "保存する", class: "btn", id: "option_submit_btn" %>
    </div>
  <% end %>
</div>